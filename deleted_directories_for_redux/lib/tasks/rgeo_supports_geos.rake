# frozen_string_literal: true

desc 'Check if RGeo supports GEOS'
task :rgeo_supports_geos do
  # ?
  # puts `which dot`
  # abort 'Error: RGeo does not support GEOS.' unless `which dot` == "/app/.apt/usr/bin/dot"
end